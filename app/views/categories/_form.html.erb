<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.text_field :name, placeholder: "Name", class:"form-control mb-2" %>
  </div>
  <div>
    <%= f.text_area :description, placeholder: "Description", class:"form-control mb-2" %>
  </div>
  <button type="submit" class="btn btn-success mx-auto mb-2">Create Category</button>
<% end %>
