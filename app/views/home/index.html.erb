<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>Agora Project</title>
        
        <!-- Latest compiled and minified CSS -->
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
        
        <!-- Latest compiled and minified JavaScript -->
        <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
    </head>
    
    <body>
        
        <% if user_signed_in? %>
            <h2>Your Articles</h2>
            <div class="row">
          <% @articles.each do |article| %>
            <% if article.user_id == current_user.id %>
              <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                  <div class="caption">
                    <h3><%= article.title %></h3>
                    <p><%= article.author %></p>
                    <p><%= article.body %></p>
                    <a role="button"><%= link_to "Read", article_path(article), class: "btn btn-default" %></a>
                  </div>
                </div>
              </div>
            <% end %>
          <% end %>
          </div>
        <% end %> 
        
        <h2>Latest Articles</h2>
            
        <div class="row">
        <% @articles.each do |article| %>
          <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
              <div class="caption">
                <h3><%= article.title %></h3>
                <p><%= article.author %></p>
                <p><%= article.body %></p>
                <a role="button"><%= link_to "Read", article_path(article), class: "btn btn-default" %></a>
              </div>
            </div>
          </div>
        <% end %>
        </div>
      
        <h2>Most Commented</h2>
        
    </body>
</html>
